name: Rebuild quiz data

# data/*_source.csv（スプレッドシートからの書き出し＝正本）が更新されたら、
# 各アプリの js データ部分を自動で生成し直してコミットする。
#  - study-tool/   … js/vocab.js・js/rika.js・js/shakai.js
#  - kenritsu-tool/ … js/rika.js・js/shakai.js

on:
  push:
    paths:
      - "study-tool/data/vocab_source.csv"
      - "study-tool/data/rika_source.csv"
      - "study-tool/data/shakai_source.csv"
      - "kenritsu-tool/data/rika_source.csv"
      - "kenritsu-tool/data/shakai_source.csv"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Rebuild study-tool js data from CSV
        run: node study-tool/tools/build-data.js

      - name: Rebuild kenritsu-tool js data from CSV
        run: node kenritsu-tool/tools/build-data.js

      - name: Commit regenerated data
        run: |
          if [[ -n "$(git status --porcelain study-tool/js kenritsu-tool/js)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add study-tool/js/vocab.js study-tool/js/rika.js study-tool/js/shakai.js \
                    kenritsu-tool/js/rika.js kenritsu-tool/js/shakai.js
            git commit -m "chore: rebuild quiz data from CSV [skip ci]"
            git push
          else
            echo "No data changes to commit."
          fi
